# Production override для VPS (Timeweb и др.)
# Запуск: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
#
# Безопасность:
# - PostgreSQL НЕ проброшен на хост (только внутри docker network)
# - API НЕ проброшен на хост (доступ через nginx внутри compose)
# - nginx слушает 0.0.0.0:80 и 0.0.0.0:8090 — публичный доступ по IP (порт 80 для ISP без нестандартных портов)

services:
  db:
    ports: !reset []

  api:
    ports: !reset []
    environment:
      NODE_ENV: production
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-https://your-domain.ru}

  nginx:
    ports:
      - "0.0.0.0:80:80"
      - "0.0.0.0:${NGINX_PORT:-8090}:80"
